{
  "name": "coffee-quest",
  "version": "1.0.0",
  "description": "This is weird...",
  "main": "index.coffee",
  "scripts": {
    "start": "coffee --compile --output build/ src/ & node build/index.js",
    "build": "coffee --compile --output build/ src/",
    "dev": "coffee -o buld/ -cw src/"
  },
  "author": "Valery Romanov",
  "license": "MIT",
  "devDependencies": {
    "babel-core": "^6.26.0",
    "babel-preset-env": "^1.6.0",
    "coffeescript": "^2.0.0"
  }
}
